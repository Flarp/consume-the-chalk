import React from "react"
import { KEYUTIL } from "jsrsasign"
import { AdminProps, ChalkboardRow } from "./types"

class Admin extends React.Component<AdminProps> {
  constructor(props: AdminProps) {
    super(props)
  }
}
